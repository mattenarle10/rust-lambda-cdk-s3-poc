# rust-lambda-cdk-s3-poc

Small POC for:
- Rust Lambda functions
- AWS CDK (TypeScript)
- API Gateway HTTP endpoints
- Simple S3 CRUD using AWS SDK for Rust (later)

This is phase 2 after `rust-lambda-poc`: a tiny HTTP micro-API in Rust.
